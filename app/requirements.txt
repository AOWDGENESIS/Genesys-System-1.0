# GENESIS SYSTEM / Zeichenwerk — Kern-Abhängigkeiten
# Python 3.10+ (getestet auf 3.11 und 3.12)

Pillow>=10.0.0
PyYAML>=6.0
requests>=2.31.0

# --- Optionale KI-Backends (System läuft auch ohne; Engines degradieren sauber) ---
huggingface_hub>=0.23.0
google-genai>=1.0.0
